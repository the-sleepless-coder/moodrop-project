mqtt:
  url: "tcp://70.12.245.101:1883"
  out:
    client-id: "SpringOutboundId"
    topic: "perfume/recipe"
    qos: 1
  in:
    client-id: "SpringInboundId"
    topic: "perfume/feedback"
    qos: 1

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/moodrop
    username: json
    password: jsonsvalue!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
     # Entity의 camelCase <-> Snake Case 
      naming:
        physical-strategy: org.hibernate.boot.model.naming.CamelCaseToUnderscoresNamingStrategy

    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: false

  elasticsearch:
    uris: http://localhost:9200

  data:
    redis:
      host: localhost
      port: 6379

mybatis:
  mapper-locations: classpath*:/mappers/**/*.xml
  type-aliases-package: com.moodrop.model.dto
  configuration:
    map-underscore-to-camel-case: true
    default-fetch-size: 100
    default-statement-timeout: 30

server:
  port: 8080
  address: 0.0.0.0

app:
  es:
    index:
      perfume: perfumes_v2

jwt:
  secret: bW9vZHJvcC1zZWNyZXQta2V5LWZvci1qd3QtdG9rZW4tZ2VuZXJhdGlvbi0yMDI0
  access-token-expiration: 1800000      # 30분 (ms)
  refresh-token-expiration: 604800000   # 7일 (ms)


scheduler:
  recipe:
    view-sync-delay: 5s

  perfume:
    like-batch-time: "30 1 0 * * *"

  enabled:
    true
#   sql:
#     init:
#       mode: always
#       separator: //
#      data-locations: classpath:data.sql

